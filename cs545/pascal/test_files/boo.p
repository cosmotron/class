program main(input,output);
 var a,b,c: integer;
 var x,y,z: real;
 var p: array[1 .. 10] of integer;
 var q: array[1 .. 10] of real;
 procedure foo(a: integer);
  var b: real;
 begin
  b := c
 end;

begin
 if (b + 1 < 0) then a := 1

end.

